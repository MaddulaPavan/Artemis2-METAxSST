# PreferenceAggregationEnv — Dependencies
# Install: pip install -r requirements.txt

# Core environment + typed models
pydantic>=2.0.0

# FastAPI HTTP server (HuggingFace Space / Docker deployment)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Baseline inference script (OpenAI-compatible client)
openai>=1.0.0

# YAML parsing (openenv validate reads openenv.yaml)
PyYAML>=6.0

# Optional: diagnostic plots (visualize.py — not required for submission)
matplotlib>=3.7.0
